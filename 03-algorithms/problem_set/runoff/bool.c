#include <stdio.h>
#include <cs50.h>

int main(void)
{
    bool name = false;
    if (!name)
    {
        printf("Eric\n");
    }

    // bool voted = false;
    // if (voted == false)
    // {
    //     printf("voted is false\n");
    // }
}
